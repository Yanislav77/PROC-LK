[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    ui: UI / E2E tests (Playwright)
    api: API tests (requests)
    smoke: smoke suite
    regression: regression suite
asyncio_mode = auto

[pytest]
testpaths = tests
addopts = -v --tb=short --html=reports/report.html --self-contained-html --browser chromium --browser firefox --browser webkit
markers =
    ui: UI / E2E tests (Playwright)
    api: API tests (requests)
    smoke: smoke suite
    regression: regression suite
asyncio_mode = auto
